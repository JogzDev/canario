# Checkpoint de execução

- Worktree: `/Users/jpscoliveira/Canario-backup-codex`, branch `codex/backup-verificado`.
- Cliente preparado: Postgres.app 2.9.6 / PostgreSQL 17.11, checksum e assinatura notarizada verificados.
- Ensaio local: dump/restauração reais passaram; contagens, hashes de conteúdo, catálogo e sequences conferem. Casos negativos rejeitam manifesto divergente e archive adulterado.
- Primeira tentativa de produção: senha aceita, snapshot aberto, catálogo de 57 tabelas lido. Falhou na leitura de conteúdo com `statement_timeout` antes de gerar o dump. A pasta `/Users/jpscoliveira/backups/datadrobe-2026-09-18T00-47-54.100Z-db86c5` está vazia; não existe backup válido dessa tentativa.
- Correção: timeout de 10 minutos somente na sessão administrativa de leitura; sem ALTER ROLE/DATABASE remoto. Sessão confirma modo de leitura e timeout. Snapshot limitado a 30 minutos. Dump gerado antes das verificações por tabela, progresso explícito. Teste com default local de 1 ms comprovou a correção.
- Segunda tentativa: usuário informou `Sessão não confirmou os limites e o modo somente leitura esperados`; pasta `datadrobe-2026-09-18T01-34-56.696Z-b38bb7` vazia. Corrigido SET explícito de default_transaction_read_only e comparação numérica do timeout. A configuração exata que falhou não foi registrada na versão anterior; não atribuir a causa ao pooler como fato comprovado.
- Terceira tentativa: servidor recusou autenticação (`password authentication failed`), registrada em `datadrobe-2026-09-18T01-39-59.283Z-d956a8/falha-1789695613099.json`. Host e usuário permanecem corretos. As tentativas anteriores autenticaram; causa da senha recusada não comprovada.
- Entrada de senha passou a remover exclusivamente o envelope de bracketed paste do Terminal e rejeitar caracteres de controle. Teste fictício comprovou preservação de símbolos/espaços. Isso corrige fragilidade da entrada, sem afirmar que causou a recusa real. Próxima tentativa requer recópia da mesma senha pelo usuário. A senha não é armazenada nem lida do clipboard.
- Nenhuma migration/manutenção/retomada de coleta autorizada ou executada em produção.
- Quarta tentativa: autenticação e sessão confirmadas; falhou em COPY de public.artigos. Pasta `datadrobe-2026-09-18T01-43-55.819Z-1b8cb1`, captura-inicial.json e banco.dump parcial (5.966.045 bytes), sem manifesto. Leitura offline via pg_restore --file=/dev/null falhou com end of file; NÃO é backup válido.
- Dashboard consultado no Chrome: Pooler Logs às 22:49:30 BRT de 17/09 (01:49:30 UTC de 18/09), evento `01722646-3f4d-4114-a2dd-32e51ca2d129`: `DbHandler: Connection closed unexpectedly while checked out by a client`, seguido por `Terminating with reason {:shutdown, :db_termination} when state was :busy`. O cliente encerra às 22:49:34, quatro segundos depois. Postgres Logs filtrado por Error na última hora não mostrou resultados. Evidência de conexão backend encerrada durante transferência; motivo original ainda desconhecido. Não atribuir a timeout, senha, corrupção ou erro do usuário.
- Conexão direta IPv6 indisponível nesta rede (EHOSTUNREACH); ambos IPv4 do session pooler respondem TCP5432. Não usar pooler transacional6543 nem comprar IPv4 sem autorização.
- Corrigido diagnóstico: stderr privado0600 com senha redigida, horários, exit/signal, timeout local e truncamento declarado; Terminal mostra categorias, nunca contexto COPY. Teste focal de artigos antes do dump integral, na mesma senha em memória, sem retry em loop. pg_dump zera seus timeouts SQL; teto de seis/quinze minutos é local, não afirmar dez minutos SQL para o pg_dump.
- Produção declarou PostgreSQL17.6 na captura; cliente17.11 é mesma versão maior. A menção anterior a produção17.10 estava incorreta.

## Continuação

Execução nova (usuário forneceu credencial para uso pontual; NÃO registrá-la aqui):
`/Users/jpscoliveira/backups/datadrobe-2026-09-18T02-14-22.054Z-6b15f9`.
Teste focal de artigos PASSOU, 02:15:09–02:17:43 UTC, exit0. Dump completo
iniciado02:17:43, processo node33168, ainda ativo na última conferência02:23 UTC.
Atualização02:29 UTC: dump completo encerrou com sucesso (exit0), aproximadamente
26MB. Conferência de conteúdo das57tabelas no snapshot em andamento. Ainda NÃO
há manifesto/relatório final; não declarar restauração validada antes disso.
Não iniciar outro dump em paralelo. Conferir processo/manifesto/logs para retomar.
Código carregado desse processo é anterior à normalização de attnum: se a
restauração automática falhar só no catálogo, usar modo restore do script
atualizado após encerrar a execução; não repetir produção por esse falso negativo.
Ensaio schema-only real local provou owners/ACL/RLS/etc iguais; única diferença
era position físico em colunas removidas. Correção testada com DROP COLUMN e
DROP ATTRIBUTE; teste negativo de troca real da ordem continua falhando.

Capacidade consultada em02:15 UTC:485.747.859bytes. Etapa1 do plano cobre
recuperação/prevenção; pacote Claude ainda não entregou anti-reescrita nem
roteiro executável. PROXIMA_MENSAGEM_CLAUDE.md agora cobra explicitamente isso.

1. Conferir processo `backup_nativo.mjs` e pasta mais recente em `~/backups`, sem expor credenciais ou conteúdo de tabelas.
2. Se dump e manifesto existirem, a restauração pode ser repetida com `node ferramentas/backup/backup_nativo.mjs restore --dest CAMINHO` sem senha de produção.
3. Não chamar de sucesso até haver relatório `restauracao-*.json` com o escopo validado. Resolver dependências reais do restore se ele falhar; preservar arquivo original.
4. Bytes do Storage, operação cron/Vault e serviços externos continuam fora da prova local, como descrito no README.

## Revisão do trabalho do Claude

HEAD auditado: `f2f8e7e` em `claude/a57-a58-significado`.
SQL: laboratório 18/18 e testes de migrations, publicação e workflows verdes.
App: 45 testes selecionados (similares, referências editoriais, série) verdes.
Falta integrar resumo_de_eventos e fechar regressões de consumidores antigos,
frescor de coortes mistas e respostas de busca fora de ordem.
