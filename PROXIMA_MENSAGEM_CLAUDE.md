Revisei os commits `88c9965`, `edab6ca` e `def05d9`. O Codex também rodou novamente, sobre o HEAD `def05d9`, o contrato das 87 migrations, o laboratório PostgreSQL 17 com 22 asserções e os 310 testes Swift; tudo passou. Não reexecutei os 12 fluxos de simulador nesta auditoria, mas a seleção offline está no workflow e registro como evidência o resultado local que você reportou. Compatibilidade v1/v2, frescor pela ponta velha, período no zero, cancelamento/identidade da busca, PGRST202 restrito, agregação da capa e fixtures de interface estão aceitos. Não refaça nem reabra esses pontos agora. Preserve os commits como estão.

As cinco pendências concretas da sua resposta ficam classificadas assim, para não se perderem no próximo bloco:

- `Localizable.xcstrings`: aceito provisoriamente como está, inclusive as duas remoções justificadas e as duas frases inseridas à mão. Não normalize nem gere novo diff agora. Antes do build de release, rodar o extrator no Xcode/IDE, revisar o diff resultante e garantir que só entre alteração relacionada ao pacote; isso é portão de release, não trabalho desta etapa de capacidade.
- GitHub Actions: os testes locais são evidência suficiente para continuar, mas não substituem o CI remoto. Como nada foi enviado, o workflow ainda não rodou. Quando houver push/PR autorizado, laboratório, lógica, build e os 12 fluxos offline precisam ficar verdes antes de publicar.
- RPCs ausentes em produção: é o estado esperado. Não trate a mensagem temporária da capa nem o silêncio editorial como regressão local. A ordem continua backend A57/A58 primeiro e app consumidor depois, somente após liberar capacidade e passar pelos portões abaixo.
- `pg_trgm`: continua adiado até existir folga real e a medição justificar o custo.
- Backup: resolvido e verificado no escopo declarado, conforme a prova abaixo; deixou de bloquear a preparação do roteiro, mas não autoriza sozinho nenhuma mutação em produção.

O backup também deixou de ser pendência. Em 18/09/2026 às 08:59 BRT, a restauração real terminou com exit 0 e resultado `VERIFICADO_NO_ESCOPO_DECLARADO`: 57 tabelas nos schemas `auth`, `public`, `storage` e `supabase_migrations`, com assinaturas de conteúdo, catálogo, permissões/RLS e sequences conferidos. O archive tem 27.774.292 bytes e SHA-256 verificado. Destino: `/Users/jpscoliveira/backups/datadrobe-2026-09-18T02-14-22.054Z-6b15f9`; relatório: `/Users/jpscoliveira/backups/datadrobe-2026-09-18T02-14-22.054Z-6b15f9/restauracao-1789732775050.json`. A comparação de conteúdo usou um snapshot posterior ao dump, declarado no relatório. A prova não inclui os bytes dos objetos do Storage nem comprova a operação de cron/Vault, Auth externo, e-mails, Edge Functions ou configurações da plataforma. Não inicie outro dump e não copie conteúdo ou credenciais do backup.

Agora feche somente a etapa 1: capacidade do banco e prevenção de crescimento. Os quatro commits novos não tocam essa frente. A medição de 18/09 às 02:33 UTC foi 485.772.435 bytes no banco `postgres`, mas 501.046.069 bytes somando `postgres`, `template0` e `template1`. O `uso_do_banco` atual mede apenas `pg_database_size(current_database())`; o portão considera limite de 500.000.000 bytes e bloqueia coleta a partir de 96%. O backup não libera espaço.

Entregue localmente, sem aplicar nada em produção:

1. Uma migration que impeça reescritas idênticas em `series_semanais` nos caminhos ainda ativos de varejo, editorial e `computar_z`. Repetir a mesma entrada deve manter os mesmos resultados sem atualizar `computado_em` nem criar novas versões de linha quando nenhum valor de negócio mudou. Trate corretamente NULL com `IS DISTINCT FROM`. Não altere o significado das séries e não apague histórico.

2. Testes executáveis que demonstrem, para cada caminho alterado, primeira escrita, reexecução idêntica com zero linhas reescritas e mudança real com somente as linhas necessárias atualizadas. Inclua o laboratório SQL real, não apenas portões de texto.

3. Uma correção da medição/alerta para reconciliar o que o portão enxerga com a métrica de capacidade mostrada pelo Supabase. Não invente que conseguimos controlar o tamanho dos bancos template; declare separadamente banco principal, overhead interno e total relevante para a cota. Se a métrica exata da plataforma não puder ser obtida por SQL do papel disponível, deixe a limitação explícita e use um cálculo conservador comprovado.

4. Um roteiro operacional executável, porém ainda não executado, com inventário somente leitura antes de qualquer ação: tamanho por tabela e índice, lixo/tuplas mortas, `cron.job_run_details`, espaço temporário exigido, locks, duração esperada, medição antes/depois e critérios de interrupção. Separe claramente: ação que apenas remove linhas, ação que devolve espaço físico e ação preventiva. `DELETE` não conta como redução física imediata. `REINDEX` e `VACUUM FULL` só podem aparecer com alvo exato, folga necessária, lock esperado e alternativa/abortamento. Se `TRUNCATE` de logs operacionais for considerado, prove escopo e consequências; não o execute.

5. Preserve dados de negócio, histórico semanal e os snapshots necessários para o backfill de `sortimento_diario`. A A58 precisa reconstruir o denominador antes de qualquer poda que alcance esses snapshots. Não proponha exclusão arbitrária, upgrade de plano, migração de provedor nem um número de economia sem medição. `pg_trgm` continua adiado até existir folga real.

6. Inclua no checklist a rotação coordenada da senha administrativa depois de inventariar todos os consumidores. Não altere a senha unilateralmente e não a copie para código, documentação, commits ou logs.

A ordem operacional a preparar é: medir novamente em leitura → revisar conosco o roteiro e os alvos → autorizar e executar a menor recuperação segura → confirmar escrita liberada e pelo menos 20% de folga real → aplicar a prevenção de reescritas → observar o crescimento → só então publicar A57/A58 no backend e depois o app consumidor. Se a migration preventiva precisar vir antes de alguma compactação para impedir que a folga volte a sumir, explique essa dependência e o custo de aplicá-la com o banco no limite; não mude a ordem silenciosamente.

Volte com os commits dessa etapa, as medições que sustentam cada alvo, os testes e o roteiro exato. Não execute migration, limpeza, compactação, retomada da coleta, publicação ou rotação de credencial em produção nesta rodada.
