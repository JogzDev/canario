Revisei os commits `0b27976` e `5d02865` contra a `main`, que é a base correta dessa branch. Rodei novamente `teste_capacidade_banco.py`, o contrato das 89 migrations, o laboratório de capacidade com 16 asserções e o ensaio do roteiro com 26 verificações; tudo passou. Conferi também a documentação oficial: a conta da P21 coincide com a métrica publicada pelo Supabase, a retenção proposta pela P23 segue o padrão recomendado pelo próprio pg_cron, e os locks/espaço temporário descritos para `VACUUM FULL` e `REINDEX` são compatíveis com o PostgreSQL 17. A medição, P21, P22, o executor por passos e os critérios de interrupção estão aceitos como base.

As três decisões ficam fechadas assim:

1. **P24 entra como guarda transitória, mas não vamos usá-la para acumular uma semana de snapshots.** Depois da recuperação e das migrations preventivas, integre e publique a A58 completa no backend antes de retomar a coleta. Ela cria e preenche `sortimento_diario` com os snapshots preservados e torna a guarda da P24 inerte; o motor passa a materializar o dia antes da poda. Isso é melhor que separar um pedaço da A58 e melhor que gastar ~2,7 MB/dia durante a observação. A57 também pode entrar no mesmo rollout de backend, porque as RPCs novas são versionadas e o consumidor antigo permanece intacto. A observação de sete dias passa a acontecer com o backend novo e a coleta retomada; o app consumidor só é publicado depois dela.

2. **O passo 60 fica autorizado apenas condicionalmente:** depois das ações anteriores, medir de verdade. Se a cota já estiver em até 400.000.000 bytes, ele não roda. Se ainda estiver acima, ele é necessário para cumprir os 20% de folga, desde que todas as pré-condições de espaço, lock, motor parado e timeout estejam verdes.

3. **P23 entra logo após o passo 11**, em vez de esperar o fim da etapa preventiva. O log é a única frente que continua crescendo com a coleta parada; depois de apagar e compactar, não faz sentido deixá-lo voltar a crescer. Antes de aplicar, acrescente uma pré-condição somente leitura que confirme extensão/função `cron.schedule`, ausência de job homônimo incompatível e usuário esperado; depois, confira que existe exatamente um job ativo com nome, agenda, banco, usuário e comando previstos. O PostgreSQL embarcado não ter pg_cron deixa de ser um bloqueio se essas pré e pós-condições de produção forem explícitas e a migration continuar falhando fechada.

Antes de qualquer produção, corrija estes dois pontos do roteiro:

- **Evite trabalho duplicado em `artigos`.** `VACUUM FULL public.artigos` reescreve a tabela e reconstrói os índices. Portanto, se o passo 60 for necessário, rodar o passo 40 antes desperdiça I/O, WAL e uma janela de lock. Transforme 40/60 numa decisão depois do passo 50: medir; se o `REINDEX artigos_url_key` sozinho for suficiente para chegar a ≤400 MB, usar apenas 40; se não for, pular 40 e usar 60, que recupera heap e índices juntos. Atualize estimativas, pré-condições e o ensaio para provar que os caminhos são mutuamente exclusivos.
- **Não compacte `indices_semanais` sem prevenir a recorrência.** O próprio inventário mostra 610.443 updates em 9.897 linhas e o roteiro admite que os ~7,3 MB voltariam. Acrescente uma migration diferencial para o caminho ativo que reescreve `indices_semanais`, com a mesma disciplina da P22: reexecução idêntica gera zero versões novas; mudança real toca apenas as linhas necessárias; NULL é tratado com `IS DISTINCT FROM`; resultado integral permanece idêntico. Cubra no laboratório executável. Se isso não for seguro nesta rodada, retire o passo 30 e seus 7,3 MB da meta de recuperação — não conte economia temporária como folga sustentável.

As demais pendências ficam classificadas assim:

- `curva_tamanhos` e `raridade_do_atributo`: meça tamanho, updates por dia e fração de reescritas idênticas. Corrija agora apenas se houver reescrita sem mudança material que ameace a folga; caso contrário, registre números e deixe em backlog. Não agrupe alteração sem prova só porque o padrão parece semelhante.
- As ~285 células editoriais/dia e a reescala do Google: são mudanças reais de valor segundo a medição; ficam fora desta correção de no-op e entram apenas na observação de crescimento.
- P23 sem pg_cron no laboratório: tratada pelas pré/pós-condições acima e pelo portão textual; não tente simular uma extensão diferente e chamar isso de prova real.
- Durações: continuam estimativas. O executor mede e os timeouts/abortamentos são a autoridade durante a janela.
- GitHub Actions: continua pendente porque não houve push. Antes de produção, a branch integrada precisa ser enviada por fluxo autorizado e todos os jobs precisam ficar verdes.
- Rotação da senha: depois de terminar recuperação e migrations de banco, antes de retomar a coleta. Inventariar consumidores externos, trocar uma vez, atualizar/verificar e registrar somente a data. Não trocar no meio de um passo.

Crie agora uma branch de integração local a partir de `claude/etapa1-capacidade` e traga os commits do pacote A57/A58 até `def05d9`. Resolva os conflitos preservando **os dois** laboratórios e **os dois** jobs de CI; a branch de capacidade nascer da `main` não pode apagar o pacote de significado, e o pacote de significado não pode apagar P21–P24. Ajuste a ordem para: recuperação física revisada → P21/P22/prevenção de `indices_semanais` + P23/P24 → A57/A58 backend → rotação coordenada → retomada da coleta → observação diária por sete dias → app consumidor. Rode na branch integrada os dois laboratórios PostgreSQL, os testes Python completos, os 310 testes Swift e os 12 fluxos offline de interface.

Volte com os commits revisados, o novo grafo de decisão 40/60, a prova da prevenção de `indices_semanais`, o resultado dos testes combinados e a sequência exata de comandos de produção. Não faça push, não aplique migration, não execute limpeza/compactação, não retome a coleta e não altere credencial nesta rodada.
