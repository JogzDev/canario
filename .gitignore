# Segredos: regra inviolavel 9. Nunca versionar.
Config.xcconfig
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/

# macOS
.DS_Store

# Pagina de revisao gerada localmente (regenerar com o script)
VERIFICACAO_TRENDS.html

# Checkpoint bruto da auditoria HTTP; o JSON final e o relatorio ficam versionados.
anexos/auditoria_links_app.jsonl

# Descoberta privada da 1.3: artefato temporario com URLs candidatas.
radar-scout.json
radar-scout-*.json
radar-scout-receipt.json
radar-scout-receipt-*.json
radar-etiqueta-facts.json
radar-etiqueta-facts-*.json
etiqueta-*.json
radar-etiqueta-revisao*.html
radar-etiqueta-review-*.json
radar-etiqueta-*-adjudicacao.html
radar-etiqueta-reviewed*.json

# Xcode (alem do que ja estava acima)
*.xcuserdatad/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
.swiftpm/xcode/package.xcworkspace/xcuserdata/

# SwiftPM
.build/

# O Xcode recria este arquivo sozinho ao abrir o .xcodeproj: ele nao descreve o
# projeto, so o estado do workspace implicito. Entrou por engano no commit do
# visual novo em 26/08 e reaparece em toda maquina que abre o projeto.
app/Canario.xcodeproj/project.xcworkspace/contents.xcworkspacedata

# Estado local da CLI Supabase (referencia do projeto e versoes remotas)
supabase/.temp/

# Dependencias locais do laboratorio descartavel da 1.3 (P1).
#
# `package.json` e `package-lock.json` sao versionados: eles FIXAM versao e
# integridade, que e o que torna a suite reproduzivel. Os binarios em si nao --
# sao ~100 MB de PostgreSQL por plataforma, baixados por `npm ci`.
ferramentas/laboratorio_radar/node_modules/

# Diagnostico preservado quando o laboratorio falha.
ferramentas/laboratorio_radar/diagnostico-*/
